# Optional: trivia API (defaults to http://localhost:3001)
VITE_API_BASE_URL=http://localhost:3001

# Admin credentials are configured server-side in `backend/.env`
# ADMIN_USERNAME=...
# ADMIN_PASSWORD=...
